var chai = require('chai');
var util = require('util');
var express = require('express');
var request = require('supertest');

var poplar = require('../lib/poplar');
var RestAdapter = require('../lib/adapters/rest');

describe('RestAdapter', function() {
});
